class ScreenConfig {
  final int maxWidth;
  final int maxHeight;
  final int maxFrameRate;

  const ScreenConfig({
    this.maxWidth = 1920,
    this.maxHeight = 1080,
    this.maxFrameRate = 30,
  });
}
